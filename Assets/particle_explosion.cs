using UnityEngine;
using System.Collections;

public class particle_explosion : MonoBehaviour
{
		public static bool boom = false;
		// Use this for initialization
		void Start ()
		{
	
		}
	
		// Update is called once per frame
		void Update ()
		{
				if (boom == true) {
						//Instantiate (this.particleSystem., transform.position, transform.rotation);
				}
		
		}
}
